{
  "name": "team-app",
  "version": "0.0.1",
  "description": "",
  "keywords": "",
  "license": "",
  "author": "Nigtellios",
  "scripts": {
    "build": "parcel build *",
    "dev": "parcel index.html --open -p 88",
    "start": "yarn build && yarn dev",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "parcel-bundler": "^1.12.4"
  },
  "dependencies": {
    "normalize": "^0.3.1",
    "sass": "^1.45.0",
    "yarn": "^1.22.17"
  }
}
